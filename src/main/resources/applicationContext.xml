<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
         http://www.springframework.org/schema/beans
         http://www.springframework.org/schema/beans/spring-beans.xsd
         http://www.springframework.org/schema/context
         http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Enable annotation-driven configuration -->
    <context:annotation-config/>

    <!-- Property placeholder configurer for handling ${...} placeholders -->
    <context:property-placeholder location="classpath:application.properties,file:./application_override.properties"
                                  ignore-resource-not-found="true"/>

    <!-- Component scanning for Whydah Statistics Service -->
    <context:component-scan base-package="net.whydah.statistics"/>

    <!-- Include Valuereporter components if needed -->
    <context:component-scan base-package="org.valuereporter"/>

    <!-- Database Configuration - using your existing application.properties values -->
    <bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${jdbc.driverClassName}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
    </bean>

    <!-- JdbcTemplate bean that Valuereporter components need -->
    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <constructor-arg ref="dataSource"/>
    </bean>

    <!-- Jackson ObjectMapper bean for JSON serialization -->
    <bean id="objectMapper" class="com.fasterxml.jackson.databind.ObjectMapper"/>

    <!-- Manual bean definitions for SecurityFilter dependencies -->
    <bean id="securityTokenService" class="java.lang.String">
        <constructor-arg value="${securitytokenservice:http://localhost:9998/tokenservice}"/>
    </bean>

    <bean id="securityTokenServiceAppId" class="java.lang.String">
        <constructor-arg value="${securitytokenservice.appid:2211}"/>
    </bean>

</beans>